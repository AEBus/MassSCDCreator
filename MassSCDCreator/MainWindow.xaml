<Window x:Class="MassSCDCreator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:MassSCDCreator.Models"
        mc:Ignorable="d"
        Title="{Binding Texts[WindowTitle]}"
        Width="1440"
        Height="940"
        MinWidth="1240"
        MinHeight="820"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="26" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>
        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>
        <Style x:Key="MutedTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
        <Style x:Key="SummaryValueTextStyle" TargetType="TextBlock" BasedOn="{StaticResource MutedTextStyle}">
            <Setter Property="LineHeight" Value="20" />
        </Style>
        <Style x:Key="StepBadgeRuntimeStyle" TargetType="Border" BasedOn="{StaticResource StepBadgeBorderStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SuccessBackgroundBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource SuccessBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="WorkflowCardBaseStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorderBrush}" />
            <Setter Property="BorderThickness" Value="1.5" />
            <Setter Property="Padding" Value="14,12" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="Root"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="16"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                            </Border>
                            <Border x:Name="SelectedMarker"
                                    Width="12"
                                    Height="12"
                                    Margin="0,10,10,0"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Top"
                                    Background="{DynamicResource AccentStrongTextBrush}"
                                    CornerRadius="6"
                                    Opacity="0" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource ControlPressedBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="WorkflowSingleCardStyle" TargetType="Button" BasedOn="{StaticResource WorkflowCardBaseStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSingleMode}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="BorderThickness" Value="3" />
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="WorkflowBatchCardStyle" TargetType="Button" BasedOn="{StaticResource WorkflowCardBaseStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsBatchMode}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="BorderThickness" Value="3" />
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="WorkflowRefreshCardStyle" TargetType="Button" BasedOn="{StaticResource WorkflowCardBaseStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsRefreshMode}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="BorderThickness" Value="3" />
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="TemplateBuiltInCardStyle" TargetType="Button" BasedOn="{StaticResource WorkflowCardBaseStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsBuiltInTemplateSelected}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="BorderThickness" Value="3" />
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="TemplateCustomCardStyle" TargetType="Button" BasedOn="{StaticResource WorkflowCardBaseStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsCustomTemplateSelected}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="BorderThickness" Value="3" />
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="TemplateCurrentCardStyle" TargetType="Button" BasedOn="{StaticResource WorkflowCardBaseStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsCurrentTemplateSelected}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="BorderThickness" Value="3" />
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="RefreshTemplateOnlyCardStyle" TargetType="Button" BasedOn="{StaticResource WorkflowCardBaseStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsRefreshTemplateOnlySelected}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="BorderThickness" Value="3" />
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="RefreshAudioOnlyCardStyle" TargetType="Button" BasedOn="{StaticResource WorkflowCardBaseStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsRefreshAudioOnlySelected}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="BorderThickness" Value="3" />
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="RefreshTemplateAndAudioCardStyle" TargetType="Button" BasedOn="{StaticResource WorkflowCardBaseStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsRefreshTemplateAndAudioSelected}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="BorderThickness" Value="3" />
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="PresetCompatibleCardStyle" TargetType="Button" BasedOn="{StaticResource WorkflowCardBaseStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsCompatiblePresetSelected}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentSelectionBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="BorderThickness" Value="3" />
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="AdvancedQualityModeCardStyle" TargetType="Button" BasedOn="{StaticResource WorkflowCardBaseStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsAdvancedQualityModeSelected}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentSelectionBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="BorderThickness" Value="3" />
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="AdvancedBitrateModeCardStyle" TargetType="Button" BasedOn="{StaticResource WorkflowCardBaseStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsAdvancedBitrateModeSelected}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentSelectionBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="BorderThickness" Value="3" />
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ThemeSystemCardStyle" TargetType="Button" BasedOn="{StaticResource WorkflowCardBaseStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSystemThemeSelected}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="BorderThickness" Value="3" />
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ThemeLightCardStyle" TargetType="Button" BasedOn="{StaticResource WorkflowCardBaseStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsLightThemeSelected}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="BorderThickness" Value="3" />
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ThemeDarkCardStyle" TargetType="Button" BasedOn="{StaticResource WorkflowCardBaseStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsDarkThemeSelected}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="BorderThickness" Value="3" />
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="{DynamicResource WindowBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="199*"/>
            <ColumnDefinition Width="89*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="24,24,24,18" Style="{StaticResource SectionCardBorderStyle}" Grid.ColumnSpan="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Style="{StaticResource SectionTitleStyle}" FontSize="30" Text="{Binding Texts[AppTitle]}" />
                    <TextBlock Style="{StaticResource MutedTextStyle}" MaxWidth="760" Text="{Binding Texts[AppSubtitle]}" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <StackPanel>
                        <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[ThemeLabel]}" />
                        <StackPanel Orientation="Horizontal">
                            <Button MinWidth="88" Margin="0,0,10,0" Style="{StaticResource ThemeSystemCardStyle}" Command="{Binding SelectSystemThemeCommand}">
                                <TextBlock FontWeight="SemiBold" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{Binding Texts[ThemeSystem]}" />
                            </Button>
                            <Button MinWidth="88" Margin="0,0,10,0" Style="{StaticResource ThemeLightCardStyle}" Command="{Binding SelectLightThemeCommand}">
                                <TextBlock FontWeight="SemiBold" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{Binding Texts[ThemeLight]}" />
                            </Button>
                            <Button MinWidth="88" Style="{StaticResource ThemeDarkCardStyle}" Command="{Binding SelectDarkThemeCommand}">
                                <TextBlock FontWeight="SemiBold" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{Binding Texts[ThemeDark]}" />
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="24,0,24,18" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*" />
                <ColumnDefinition Width="340" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource SectionCardBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <ItemsControl Grid.Row="0" ItemsSource="{Binding Steps}" Margin="0,0,0,20">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Rows="1" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource StepBadgeRuntimeStyle}" Margin="0,0,12,0" Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" Foreground="{DynamicResource SecondaryTextBrush}" Text="{Binding Number}" />
                                        <TextBlock Margin="0,6,0,0" FontWeight="SemiBold" Text="{Binding DisplayTitle}" />
                                        <TextBlock Margin="0,4,0,0" Style="{StaticResource MutedTextStyle}" Text="{Binding DisplayDescription}" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <Grid>
                            <Border Style="{StaticResource InfoCardBorderStyle}" Visibility="{Binding IsWorkflowStepActive, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource SectionTitleStyle}" Text="{Binding Texts[WorkflowHeading]}" />
                                    <TextBlock Style="{StaticResource MutedTextStyle}" Margin="0,0,0,18" Text="{Binding Texts[WorkflowHint]}" />
                                    <UniformGrid Columns="3">
                                        <Button Margin="0,0,14,0" Style="{StaticResource WorkflowSingleCardStyle}" Command="{Binding SelectSingleModeCommand}">
                                            <StackPanel>
                                                <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{Binding Texts[ModeSingleTitle]}" />
                                                <TextBlock Margin="0,8,0,0" TextWrapping="Wrap" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Opacity="0.92" Text="{Binding Texts[ModeSingleDesc]}" />
                                            </StackPanel>
                                        </Button>
                                        <Button Margin="0,0,14,0" Style="{StaticResource WorkflowBatchCardStyle}" Command="{Binding SelectBatchModeCommand}">
                                            <StackPanel>
                                                <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{Binding Texts[ModeBatchTitle]}" />
                                                <TextBlock Margin="0,8,0,0" TextWrapping="Wrap" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Opacity="0.92" Text="{Binding Texts[ModeBatchDesc]}" />
                                            </StackPanel>
                                        </Button>
                                        <Button Style="{StaticResource WorkflowRefreshCardStyle}" Command="{Binding SelectRefreshModeCommand}">
                                            <StackPanel>
                                                <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{Binding Texts[ModeRefreshTitle]}" />
                                                <TextBlock Margin="0,8,0,0" TextWrapping="Wrap" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Opacity="0.92" Text="{Binding Texts[ModeRefreshDesc]}" />
                                            </StackPanel>
                                        </Button>
                                    </UniformGrid>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource InfoCardBorderStyle}" Visibility="{Binding IsPathsStepActive, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource SectionTitleStyle}" Text="{Binding Texts[PathsHeading]}" />
                                    <TextBlock Style="{StaticResource MutedTextStyle}" Margin="0,0,0,18" Text="{Binding Texts[PathsHint]}" />
                                    <Border Style="{StaticResource DangerCardBorderStyle}" Margin="0,0,0,18" Visibility="{Binding HasPathsIssues, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <ItemsControl ItemsSource="{Binding PathsIssues}" />
                                    </Border>
                                    <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding InputLabel}" />
                                    <Grid Margin="0,0,0,14">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBox Margin="0,0,12,0" Text="{Binding InputPath, UpdateSourceTrigger=PropertyChanged}" />
                                        <Button Grid.Column="1" MinWidth="120" Command="{Binding BrowseInputCommand}" Content="{Binding Texts[BrowseButton]}" />
                                    </Grid>
                                    <CheckBox IsChecked="{Binding RecursiveSearchEnabled}" Visibility="{Binding IsSingleMode, Converter={StaticResource InverseBooleanToVisibilityConverter}}" Content="{Binding Texts[RecursiveSearch]}" />
                                    <StackPanel Margin="0,18,0,0" Visibility="{Binding ShowOutputSelection, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding OutputLabel}" />
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBox Margin="0,0,12,0" Text="{Binding OutputPath, UpdateSourceTrigger=PropertyChanged}" />
                                            <Button Grid.Column="1" MinWidth="120" Command="{Binding BrowseOutputCommand}" Content="{Binding Texts[BrowseButton]}" />
                                        </Grid>
                                    </StackPanel>
                                    <Border Style="{StaticResource WarningCardBorderStyle}" Margin="0,18,0,0" Visibility="{Binding ShowOutputSelection, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                        <TextBlock Foreground="{DynamicResource WarningTextBrush}" TextWrapping="Wrap" Text="{Binding Texts[RefreshInPlaceHint]}" />
                                    </Border>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource InfoCardBorderStyle}" Visibility="{Binding IsTemplateAudioStepActive, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource SectionTitleStyle}" Text="{Binding Texts[TemplateAudioHeading]}" />
                                    <Border Style="{StaticResource DangerCardBorderStyle}" Margin="0,0,0,18" Visibility="{Binding HasTemplateIssues, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <ItemsControl ItemsSource="{Binding TemplateIssues}" />
                                    </Border>
                                    <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[TemplateSectionTitle]}" />
                                    <Button Margin="0,0,0,12" Style="{StaticResource TemplateBuiltInCardStyle}" Command="{Binding SelectBuiltInTemplateCommand}">
                                        <StackPanel>
                                            <TextBlock FontSize="17" FontWeight="SemiBold" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{Binding Texts[TemplateBuiltInTitle]}" />
                                            <TextBlock Margin="0,6,0,0" TextWrapping="Wrap" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Opacity="0.92" Text="{Binding Texts[TemplateBuiltInDesc]}" />
                                        </StackPanel>
                                    </Button>
                                    <Button Margin="0,0,0,12" Style="{StaticResource TemplateCustomCardStyle}" Command="{Binding SelectCustomTemplateCommand}">
                                        <StackPanel>
                                            <TextBlock FontSize="17" FontWeight="SemiBold" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{Binding Texts[TemplateCustomTitle]}" />
                                            <TextBlock Margin="0,6,0,0" TextWrapping="Wrap" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Opacity="0.92" Text="{Binding Texts[TemplateCustomDesc]}" />
                                        </StackPanel>
                                    </Button>
                                    <Button Style="{StaticResource TemplateCurrentCardStyle}" Command="{Binding SelectCurrentTemplateCommand}" Visibility="{Binding ShowTemplateCurrentOption, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <StackPanel>
                                            <TextBlock FontSize="17" FontWeight="SemiBold" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{Binding Texts[TemplateCurrentTitle]}" />
                                            <TextBlock Margin="0,6,0,0" TextWrapping="Wrap" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Opacity="0.92" Text="{Binding Texts[TemplateCurrentDesc]}" />
                                        </StackPanel>
                                    </Button>

                                    <StackPanel Margin="0,18,0,0" Visibility="{Binding ShowTemplatePathSelection, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[TemplatePathLabel]}" />
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBox Margin="0,0,12,0" Text="{Binding TemplateScdPath, UpdateSourceTrigger=PropertyChanged}" />
                                            <Button Grid.Column="1" MinWidth="120" Command="{Binding BrowseTemplateCommand}" Content="{Binding Texts[BrowseButton]}" />
                                        </Grid>
                                        <TextBlock Margin="0,10,0,0" Style="{StaticResource MutedTextStyle}" Text="{Binding Texts[TemplatePathHint]}" />
                                    </StackPanel>

                                    <StackPanel Margin="0,24,0,0" Visibility="{Binding ShowRefreshActionSelection, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[RefreshActionTitle]}" />
                                        <TextBlock Margin="0,0,0,12" Style="{StaticResource MutedTextStyle}" Text="{Binding Texts[RefreshActionHint]}" />
                                        <Button Margin="0,0,0,12" Style="{StaticResource RefreshTemplateOnlyCardStyle}" Command="{Binding SelectRefreshTemplateOnlyCommand}">
                                            <StackPanel>
                                                <TextBlock FontSize="17" FontWeight="SemiBold" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{Binding Texts[RefreshActionTemplateOnlyTitle]}" />
                                                <TextBlock Margin="0,6,0,0" TextWrapping="Wrap" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Opacity="0.92" Text="{Binding Texts[RefreshActionTemplateOnlyDesc]}" />
                                            </StackPanel>
                                        </Button>
                                        <Button Margin="0,0,0,12" Style="{StaticResource RefreshAudioOnlyCardStyle}" Command="{Binding SelectRefreshAudioOnlyCommand}">
                                            <StackPanel>
                                                <TextBlock FontSize="17" FontWeight="SemiBold" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{Binding Texts[RefreshActionAudioOnlyTitle]}" />
                                                <TextBlock Margin="0,6,0,0" TextWrapping="Wrap" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Opacity="0.92" Text="{Binding Texts[RefreshActionAudioOnlyDesc]}" />
                                            </StackPanel>
                                        </Button>
                                        <Button Style="{StaticResource RefreshTemplateAndAudioCardStyle}" Command="{Binding SelectRefreshTemplateAndAudioCommand}">
                                            <StackPanel>
                                                <TextBlock FontSize="17" FontWeight="SemiBold" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{Binding Texts[RefreshActionTemplateAndAudioTitle]}" />
                                                <TextBlock Margin="0,6,0,0" TextWrapping="Wrap" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Opacity="0.92" Text="{Binding Texts[RefreshActionTemplateAndAudioDesc]}" />
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>

                                    <StackPanel Visibility="{Binding ShowAudioEncodingOptions, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Margin="0,24,0,8" Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[AudioRecommendedTitle]}" />
                                        <Button Style="{StaticResource PresetCompatibleCardStyle}" Command="{Binding SelectCompatiblePresetCommand}">
                                            <StackPanel>
                                                <TextBlock FontSize="17" FontWeight="SemiBold" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{Binding Texts[PresetCompatibleTitle]}" />
                                                <TextBlock Margin="0,6,0,0" TextWrapping="Wrap" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Opacity="0.9" Text="{Binding Texts[PresetCompatibleDesc]}" />
                                            </StackPanel>
                                        </Button>

                                        <TextBlock Margin="0,18,0,8" Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[FfmpegLabel]}" />
                                        <Grid Margin="0,0,0,10">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBox Margin="0,0,12,0" Text="{Binding FfmpegPath, UpdateSourceTrigger=PropertyChanged}" />
                                            <Button Grid.Column="1" MinWidth="120" Command="{Binding BrowseFfmpegCommand}" Content="{Binding Texts[BrowseButton]}" />
                                        </Grid>
                                        <WrapPanel Margin="0,0,0,10">
                                            <Button MinWidth="180" Command="{Binding DownloadOrUpdateFfmpegCommand}" Content="{Binding Texts[FfmpegInstallButton]}" />
                                            <Button Margin="12,0,0,0" MinWidth="160" Command="{Binding ResetFfmpegStartupCheckCommand}" Content="{Binding Texts[FfmpegResetStartupCheckButton]}" />
                                        </WrapPanel>
                                        <TextBlock Margin="0,0,0,14" Style="{StaticResource MutedTextStyle}" Text="{Binding Texts[FfmpegHint]}" />
                                        <TextBlock Margin="0,0,0,10"
                                               Style="{StaticResource MutedTextStyle}"
                                               Visibility="{Binding ShowFfmpegStatus, Converter={StaticResource BooleanToVisibilityConverter}}"
                                               Text="{Binding FfmpegInstallStatus}" />

                                        <CheckBox Margin="0,20,0,0" IsChecked="{Binding ShowAdvancedAudioOptions}" Content="{Binding Texts[AudioAdvancedTitle]}" />
                                        <Border Style="{StaticResource SubtleCardBorderStyle}" Margin="0,14,0,0" Visibility="{Binding ShowAdvancedAudioOptions, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <StackPanel>
                                                <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[AdvancedModeQuality]}" />
                                                <Grid Margin="0,0,0,12">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="*" />
                                                    </Grid.ColumnDefinitions>
                                                    <Button Margin="0,0,12,0" Style="{StaticResource AdvancedQualityModeCardStyle}" Command="{Binding SelectAdvancedQualityModeCommand}">
                                                        <TextBlock FontWeight="SemiBold" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{Binding Texts[AdvancedModeQuality]}" />
                                                    </Button>
                                                    <Button Grid.Column="1" Style="{StaticResource AdvancedBitrateModeCardStyle}" Command="{Binding SelectAdvancedBitrateModeCommand}">
                                                        <TextBlock FontWeight="SemiBold" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{Binding Texts[AdvancedModeBitrate]}" />
                                                    </Button>
                                                </Grid>
                                                <TextBlock Margin="0,12,0,8" Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[AdvancedValueLabel]}" />
                                                <TextBox Width="180" Text="{Binding AdvancedValue, UpdateSourceTrigger=PropertyChanged}" />
                                                <TextBlock Margin="0,10,0,0" Style="{StaticResource MutedTextStyle}" Text="{Binding AdvancedModeHint}" />
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                    <CheckBox Margin="0,20,0,0" IsChecked="{Binding EnableLoop}" Content="{Binding Texts[LoopToggle]}" />
                                    <TextBlock Margin="0,6,0,0" Style="{StaticResource MutedTextStyle}" Text="{Binding Texts[LoopHint]}" />
                                    <CheckBox Margin="0,18,0,0" IsChecked="{Binding SaveIntermediateOggFiles}" Visibility="{Binding ShowIntermediateOggOption, Converter={StaticResource BooleanToVisibilityConverter}}" Content="{Binding Texts[SaveOggToggle]}" />
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource InfoCardBorderStyle}" Visibility="{Binding IsPenumbraStepActive, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource SectionTitleStyle}" Text="{Binding Texts[PenumbraHeading]}" />
                                    <TextBlock Style="{StaticResource MutedTextStyle}" Margin="0,0,0,18" Text="{Binding Texts[PenumbraHint]}" />
                                    <Border Style="{StaticResource DangerCardBorderStyle}" Margin="0,0,0,18" Visibility="{Binding HasPenumbraIssues, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <ItemsControl ItemsSource="{Binding PenumbraIssues}" />
                                    </Border>
                                    <CheckBox IsChecked="{Binding PenumbraExportEnabled}" Content="{Binding Texts[PenumbraEnable]}" />
                                    <StackPanel Margin="0,18,0,0" Visibility="{Binding ShowPenumbraSettings, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[PenumbraModRootLabel]}" />
                                        <Grid Margin="0,0,0,18">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBox Margin="0,0,12,0" Text="{Binding PenumbraModRootPath, UpdateSourceTrigger=PropertyChanged}" />
                                            <Button Grid.Column="1" MinWidth="120" Command="{Binding BrowsePenumbraModRootCommand}" Content="{Binding Texts[BrowseButton]}" />
                                        </Grid>
                                        <Grid Margin="0,0,0,18">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Button Margin="0,0,12,0" Style="{StaticResource SelectionCardButtonStyle}" Tag="{Binding IsCreatePlaylistModeSelected}" Command="{Binding SelectCreatePlaylistModeCommand}">
                                                <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{Binding Texts[PenumbraCreateMode]}" />
                                            </Button>
                                            <Button Grid.Column="1" Style="{StaticResource SelectionCardButtonStyle}" Tag="{Binding IsAppendPlaylistModeSelected}" Command="{Binding SelectAppendPlaylistModeCommand}">
                                                <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Text="{Binding Texts[PenumbraAppendMode]}" />
                                            </Button>
                                        </Grid>
                                        <StackPanel Visibility="{Binding ShowCreatePlaylistFields, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[PlaylistNameLabel]}" />
                                            <TextBox Text="{Binding PenumbraPlaylistName, UpdateSourceTrigger=PropertyChanged}" />
                                        </StackPanel>
                                        <StackPanel Visibility="{Binding ShowAppendPlaylistFields, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[ExistingPlaylistLabel]}" />
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBox Margin="0,0,12,0" Text="{Binding ExistingPenumbraPlaylistPath, UpdateSourceTrigger=PropertyChanged}" />
                                                <Button Grid.Column="1" MinWidth="120" Command="{Binding BrowseExistingPenumbraPlaylistCommand}" Content="{Binding Texts[BrowseButton]}" />
                                            </Grid>
                                            <Border Style="{StaticResource SubtleCardBorderStyle}" Margin="0,12,0,0" Visibility="{Binding ShowExistingPlaylistSummary, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <TextBlock Style="{StaticResource MutedTextStyle}" Text="{Binding ExistingPenumbraPlaylistSummary}" />
                                            </Border>
                                            <Border Style="{StaticResource WarningCardBorderStyle}" Margin="0,12,0,0" Visibility="{Binding ShowInferredRelativeFolderWarning, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <TextBlock Foreground="{DynamicResource WarningTextBrush}" Text="{Binding InferredRelativeFolderWarning}" TextWrapping="Wrap" />
                                            </Border>
                                        </StackPanel>
                                        <TextBlock Margin="0,18,0,8" Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[RelativeFolderLabel]}" />
                                        <TextBox Text="{Binding PenumbraRelativeScdFolder, UpdateSourceTrigger=PropertyChanged}" />
                                        <TextBlock Margin="0,8,0,0" Style="{StaticResource MutedTextStyle}" Text="{Binding Texts[RelativeFolderHint]}" />
                                        <TextBlock Margin="0,18,0,8" Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[GamePathsLabel]}" />
                                        <TextBox MinHeight="110" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Text="{Binding PenumbraGamePathsText, UpdateSourceTrigger=PropertyChanged}" />
                                        <TextBlock Margin="0,8,0,0" Style="{StaticResource MutedTextStyle}" Text="{Binding Texts[GamePathsHint]}" />
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource InfoCardBorderStyle}" Visibility="{Binding IsReviewStepActive, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource SectionTitleStyle}" Text="{Binding Texts[ReviewHeading]}" />
                                    <TextBlock Style="{StaticResource MutedTextStyle}" Margin="0,0,0,18" Text="{Binding Texts[ReviewHint]}" />
                                    <Border Style="{StaticResource DangerCardBorderStyle}" Visibility="{Binding HasReviewIssues, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <StackPanel>
                                            <TextBlock FontSize="18" FontWeight="SemiBold" Text="{Binding Texts[ReviewBlockedTitle]}" />
                                            <TextBlock Margin="0,8,0,12" Style="{StaticResource MutedTextStyle}" Text="{Binding Texts[ReviewBlockedDesc]}" />
                                            <ItemsControl ItemsSource="{Binding ReviewIssues}" />
                                        </StackPanel>
                                    </Border>
                                    <Border Style="{StaticResource InfoCardBorderStyle}" Margin="0,16,0,0" Visibility="{Binding HasReviewIssues, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                        <StackPanel>
                                            <TextBlock FontSize="18" FontWeight="SemiBold" Text="{Binding Texts[ReviewReadyTitle]}" />
                                            <TextBlock Margin="0,8,0,0" Style="{StaticResource MutedTextStyle}" Text="{Binding Texts[ReviewReadyDesc]}" />
                                        </StackPanel>
                                    </Border>
                                    <TextBlock Margin="0,20,0,10" Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[ReviewChecklistTitle]}" />
                                    <Border Style="{StaticResource SubtleCardBorderStyle}">
                                        <TextBlock Text="{Binding ReviewChecklistText}" TextWrapping="Wrap" />
                                    </Border>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource InfoCardBorderStyle}" Visibility="{Binding IsResultStepActive, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource SectionTitleStyle}" Text="{Binding Texts[ResultHeading]}" />
                                    <TextBlock FontSize="20" FontWeight="SemiBold" Margin="0,0,0,18" Text="{Binding ResultHeadline}" />
                                    <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[ResultProgressLabel]}" />
                                    <ProgressBar Maximum="100" Value="{Binding ProgressPercent}" />
                                    <TextBlock Margin="0,10,0,0" Style="{StaticResource MutedTextStyle}" Text="{Binding ProgressText}" />
                                    <TextBlock Margin="0,18,0,8" Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[ResultSummaryLabel]}" />
                                    <Border Style="{StaticResource SubtleCardBorderStyle}">
                                        <StackPanel>
                                            <TextBlock FontWeight="SemiBold" Text="{Binding StatusText}" />
                                            <TextBlock Margin="0,8,0,0" Style="{StaticResource MutedTextStyle}" Text="{Binding SummaryText}" />
                                        </StackPanel>
                                    </Border>
                                    <TextBlock Margin="0,18,0,10" Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[ResultActionsHeading]}" />
                                    <WrapPanel>
                                        <Button Margin="0,0,12,12" Command="{Binding OpenOutputFolderCommand}" Content="{Binding Texts[OpenOutputFolder]}" />
                                        <Button Margin="0,0,12,12" Command="{Binding OpenPlaylistCommand}" Content="{Binding Texts[OpenPlaylist]}" />
                                        <Button Margin="0,0,12,12" Command="{Binding ToggleLogCommand}" Content="{Binding Texts[ShowLog]}" />
                                    </WrapPanel>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </ScrollViewer>
                </Grid>
            </Border>

            <Border Grid.Column="1" Margin="18,0,0,0" Style="{StaticResource SectionCardBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock FontSize="22" FontWeight="Bold" Text="{Binding Texts[SummaryTitle]}" />

                    <ScrollViewer Grid.Row="1"
                                  Margin="0,18,0,0"
                                  Padding="0,6,14,6"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <StackPanel>
                            <Border Style="{StaticResource SubtleCardBorderStyle}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[SummaryMode]}" />
                                    <TextBlock Style="{StaticResource SummaryValueTextStyle}" Text="{Binding SummaryModeValue}" />
                                </StackPanel>
                            </Border>
                            <Border Margin="0,12,0,0" Style="{StaticResource SubtleCardBorderStyle}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[SummaryInput]}" />
                                    <TextBlock Style="{StaticResource SummaryValueTextStyle}" Text="{Binding SummaryInputValue}" />
                                </StackPanel>
                            </Border>
                            <Border Margin="0,12,0,0" Style="{StaticResource SubtleCardBorderStyle}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[SummaryOutput]}" />
                                    <TextBlock Style="{StaticResource SummaryValueTextStyle}" Text="{Binding SummaryOutputValue}" />
                                </StackPanel>
                            </Border>
                            <Border Margin="0,12,0,0" Style="{StaticResource SubtleCardBorderStyle}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[SummaryTemplate]}" />
                                    <TextBlock Style="{StaticResource SummaryValueTextStyle}" Text="{Binding SummaryTemplateValue}" />
                                </StackPanel>
                            </Border>
                            <Border Margin="0,12,0,0" Style="{StaticResource SubtleCardBorderStyle}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[SummaryAudio]}" />
                                    <TextBlock Style="{StaticResource SummaryValueTextStyle}" Text="{Binding SummaryAudioValue}" />
                                </StackPanel>
                            </Border>
                            <Border Margin="0,12,0,0" Style="{StaticResource SubtleCardBorderStyle}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[SummaryExport]}" />
                                    <TextBlock Style="{StaticResource SummaryValueTextStyle}" Text="{Binding SummaryPenumbraValue}" />
                                </StackPanel>
                            </Border>
                            <Border Margin="0,12,0,0" Style="{StaticResource SubtleCardBorderStyle}" Background="{DynamicResource SuccessBackgroundBrush}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding Texts[SummaryValidation]}" />
                                    <TextBlock Foreground="{DynamicResource SuccessBrush}" FontWeight="SemiBold" TextWrapping="Wrap" Text="{Binding SummaryValidationValue}" />
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Margin="24,0,24,12" Style="{StaticResource SectionCardBorderStyle}" Grid.ColumnSpan="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <Button MinWidth="120" Margin="0,0,12,0" Command="{Binding PreviousStepCommand}" Content="{Binding Texts[BackButton]}" />
                    <Button MinWidth="120" Margin="0,0,12,0" Command="{Binding NextStepCommand}" Content="{Binding Texts[NextButton]}" />
                    <Button MinWidth="160" Margin="0,0,12,0" Style="{StaticResource PrimaryActionButtonStyle}" Command="{Binding StartCommand}" Content="{Binding Texts[StartButton]}" />
                    <Button MinWidth="120" Command="{Binding CancelCommand}" Content="{Binding Texts[CancelButton]}" />
                </StackPanel>
                <Button Grid.Column="1" MinWidth="140" Command="{Binding ToggleLogCommand}" Content="{Binding Texts[ShowLog]}" />
            </Grid>
        </Border>

        <Expander Grid.Row="3" Margin="24,0,24,24" IsExpanded="{Binding IsLogExpanded}" Header="{Binding Texts[LogTitle]}" Grid.ColumnSpan="2">
            <Border Style="{StaticResource SectionCardBorderStyle}">
                <TextBox Height="280"
                         FontFamily="Consolas"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Visible"
                         HorizontalScrollBarVisibility="Auto"
                         Text="{Binding LogText}" />
            </Border>
        </Expander>
    </Grid>
</Window>
